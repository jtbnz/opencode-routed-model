{
  "name": "opencode-routed-model",
  "version": "1.0.9",
  "description": "OpenCode plugin that displays which model was selected by a LiteLLM auto router (complexity router, semantic router, etc.)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "tsc"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "litellm",
    "auto-router",
    "complexity-router",
    "llm-routing"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jtbnz/opencode-routed-model.git"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
